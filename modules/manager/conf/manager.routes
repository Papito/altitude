# MANAGER Routes
# This file defines MANAGER application routes (Higher priority routes first)
# ~~~~

# API
GET     /api                        controllers.manager.api.Root.index

# Client reference application
GET     /							controllers.manager.web.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.manager.web.Assets.versioned(path="/public", file: Asset)


->  / common.Routes
