version: '3'
services:
  altitude_core_postgres_test_db:
    build: .
    ports:
      - "5433:5432"
    environment:
      - POSTGRRES_NAME=altitude-test
      - POSTGRES_USER=altitude-test
      - POSTGRES_PASSWORD=dba
    container_name: altitude-core-postgres-test
