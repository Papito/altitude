const Const = {
    events: {
        folderMoved: "FOLDER_MOVED_EVENT",
    },

    attributes: {
        expanded: "expanded",
        // CAREFUL! This is still hard-coded, once, in the root folder element
        isRoot: "is-root",
    }
}
